# Vendor Assets

This folder stores local third-party libraries when CDN is not used.

Current project uses CDN for Bootstrap and Chart.js in `templates/base.html`.
You can place offline copies here later, for example:
- bootstrap.min.css
- bootstrap.bundle.min.js
- chart.umd.min.js

Keeping this README ensures the folder is versioned and non-empty.
